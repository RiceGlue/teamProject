# 정보 보안의 3대 요소 (기밀성, 무결성, 가용성) 와 프로젝트 적용 사례

## 정보 보안의 3대 요소 (CIA Triad)

### 1. 기밀성 (Confidentiality)
- **의미**: **인가된 사용자만** 정보에 접근할 수 있도록 보장하는 것입니다. 즉, 허락되지 않은 사람은 데이터를 보거나 알 수 없어야 합니다.
- **핵심 질문**: "비밀이 잘 지켜지고 있는가?"
- **예시**: 비밀번호, 개인정보, 신용카드 번호 등 민감한 정보는 암호화하여 저장하고, 특정 권한을 가진 사람만 볼 수 있도록 접근을 제어하는 것.

### 2. 무결성 (Integrity)
- **의미**: 데이터가 **인가된 사용자에 의해서만** 수정될 수 있으며, 전송 또는 저장 중에 **위변조되지 않았음**을 보장하는 것입니다. 데이터의 정확성과 완전성을 유지하는 것이 목표입니다.
- **핵심 질문**: "데이터가 정확하고 믿을 만한가?"
- **예시**: 사용자가 회원 정보를 수정할 때, 본인 인증을 거쳐야만 수정할 수 있도록 하는 것. 데이터베이스에 저장된 금액이나 예약 정보가 임의로 변경되지 않도록 보호하는 것.

### 3. 가용성 (Availability)
- **의미**: **인가된 사용자가 필요할 때** 정보와 컴퓨팅 서비스에 접근하여 사용할 수 있도록 보장하는 것입니다. 시스템이 중단 없이 안정적으로 운영되어야 함을 의미합니다.
- **핵심 질문**: "시스템을 필요할 때 문제없이 사용할 수 있는가?"
- **예시**: 사용자가 언제든지 웹사이트에 접속하여 예약하거나 웨이팅을 걸 수 있는 것. 디도스(DDoS) 공격과 같은 서비스 거부 공격을 방어하여 서버가 다운되지 않도록 유지하는 것.

---

## 우리 프로젝트(`riceglue`)에서의 적용 사례

### 기밀성 (Confidentiality)

- **회원 비밀번호 암호화**:
  - `members` 테이블의 `login_pw` 컬럼은 `VARCHAR(255)`로 설정되어 있습니다. 이는 비밀번호를 원문 그대로 저장하는 것이 아니라, **해시(Hash) 함수(예: bcrypt)를 통해 암호화하여 저장**하기 위함입니다. (Spring Security가 이 역할을 수행합니다.)
  - **해당 부분**: `members` 테이블 스키마, Spring Security 설정 및 로그인/회원가입 로직.

- **개인정보 접근 제어**:
  - 사용자는 자신의 정보만 볼 수 있어야 합니다. `마이페이지` 기능은 Spring Security의 `@AuthenticationPrincipal` 등을 통해 현재 로그인한 사용자의 정보만 조회하여 모델에 담아줍니다. 다른 사용자의 정보에는 접근할 수 없습니다.
  - **해당 부분**: `MemberController`의 `mypage()`, `editProfileForm()` 메소드.

- **역할 기반 접근 제어 (RBAC)**:
  - `members` 테이블의 `role` 컬럼(`USER`, `OWNER`, 'ADMIN')을 통해 사용자, 점주, 관리자를 구분합니다. Spring Security는 이 `role`을 기반으로 특정 URL이나 기능에 대한 접근 권한을 제어합니다.
  - **해당 부분**: Spring Security 설정, `members` 테이블의 `role` 컬럼.

### 무결성 (Integrity)

- **데이터베이스 제약 조건**:
  - `members` 테이블의 `phone`, `email`, `login_id` 등에 `UNIQUE INDEX`가 설정되어 있습니다. 이는 데이터베이스 수준에서 **중복된 데이터가 삽입되는 것을 막아** 데이터의 일관성과 정확성을 보장합니다.
  - `FOREIGN KEY` 제약 조건은 **잘못된 데이터 관계를 방지**합니다.
  - **해당 부분**: SQL 스크립트의 `CREATE TABLE` 구문 전체 (UNIQUE, FOREIGN KEY, NOT NULL 등).

- **본인 인증 후 정보 수정**:
  - `MemberController`의 `editProfile()` 메소드는 `@AuthenticationPrincipal`을 통해 현재 로그인한 사용자의 ID를 안전하게 가져와서 해당 사용자의 정보만 수정하도록 로직이 구성되어 있습니다. **서버 측에서 인증된 사용자 정보를 기준**으로 삼아 데이터의 무결성을 지킵니다.
  - **해당 부분**: `MemberController`의 `editProfile()` 메소드.

- **reCAPTCHA를 통한 악의적 가입 방지**:
  - 회원가입 시 reCAPTCHA를 사용하여 봇(Bot)에 의한 비정상적인 대량 가입을 막습니다. 이는 **유령 회원이 생성되어 데이터베이스를 오염시키는 것을 방지**하여 데이터의 무결성을 유지하는 데 기여합니다.
  - **해당 부분**: `MemberController`의 `join()`, `joinSocial()` 메소드 내 reCAPTCHA 검증 로직.

### 가용성 (Availability)

- **웹 서버(WAS) 및 데이터베이스 서버 운영**:
  - 프로젝트를 배포하여 Tomcat, MySQL 등의 서버를 실행하는 것 자체가 가용성을 보장하기 위한 기본 단계입니다. 사용자는 웹 브라우저를 통해 24시간 서비스에 접근할 수 있어야 합니다.
  - **해당 부분**: Tomcat, Nginx 등 웹 서버 및 WAS, MySQL 데이터베이스 서버의 실행과 관리.

- **로드 밸런싱 및 이중화 (고급)**:
  - (향후 확장 시 고려) 여러 대의 서버에 로드 밸런서를 이용해 트래픽을 분산시켜 일부 서버에 장애가 발생하더라도 서비스 중단을 막을 수 있습니다. 이는 가용성을 높이는 대표적인 방법입니다.
  - **해당 부분**: 인프라 아키텍처.

<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.spring.teamProject.dao.StoreDAO">
	<resultMap id="storelist" type="com.spring.teamProject.vo.StoreVO">
		<result property="storeId" column="storeId"/>
		<result property="ownerId" column="ownerId"/>
		<result property="storeName" column="storeName"/>
		<result property="storePhoneNumber" column="storePhoneNumber"/>
		<result property="address" column="address"/>
		<result property="region" column="region"/>
		<result property="storeType" column="storeType"/>
		<result property="description" column="description"/>
		<result property="operationType" column="operationType"/>
		<result property="createdAt" column="createdAt"/>
		<result property="updatedAt" column="updatedAt"/>
		<result property="avgRating" column="avgRating"/>
		<result property="countRating" column="countRating"/>
	</resultMap>
	
	<select id="SelectStoreInfo" resultMap="storelist" parameterType="String">
		<![CDATA[
			select * from stores where store_id = #{store_id}
		]]>
	</select>
	
	<select id="SelectStoreRegion" parameterType="String" resultMap="storelist">
		<![CDATA[
			select s.store_name, s.description, avg(r.rating) as 'avgRating', count(r.review_id) as 'countRating', s.store_type, s.region, s.operation_type
			from stores s inner join reviews r on s.store_id = r.store_id
			where region = #{region}
			order by s.store_id
		]]>
	</select>
	
<!--	<select id="SelectKeywordStore" parameterType="String" resultMap="storelist">-->
<!--	  <![CDATA[-->
<!--	    	select s.store_name, s.description, avg(r.rating) as '평균별점', count(r.review_id) as '리뷰개수', s.store_type, s.region, s.operation_type-->
<!--			from stores s inner join reviews r on s.store_id = r.store_id-->
<!--			where store_name =#{keyword} or -->
<!--			order by s.store_id-->
<!--	  ]]>-->
<!--	</select>-->
	
</mapper>
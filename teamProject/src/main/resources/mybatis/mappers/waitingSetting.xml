<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.teamProject.dao.WaitingSettingDAO">
    <resultMap id="WaitingSettingResultMap" type="com.spring.teamProject.vo.WaitingSettingVO">
        <id property="settingId" column="setting_id"/>
        <result property="storeId" column="store_id"/>
        <result property="dayOfWeek" column="day_of_week"/>
        <result property="maxTeams" column="max_teams"/>
        <result property="active" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="getAllSettingsByStoreId" parameterType="long" resultMap="WaitingSettingResultMap">
        SELECT
            setting_id,
            store_id,
            day_of_week,
            -- time_slot,  max_teams,
            is_active,
            created_at,  updated_at
        FROM waiting_settings
        WHERE store_id = #{storeId}
    </select>

    <select id="getSetting" resultMap="WaitingSettingResultMap">
        SELECT
            setting_id,
            store_id,
            day_of_week,
            -- time_slot,  max_teams,
            is_active,
            created_at,  updated_at
        FROM waiting_settings
        WHERE store_id = #{storeId}
          AND day_of_week = #{dayOfWeek}
          -- AND time_slot = #{timeSlot} </select>

	<select id="getSettingById" parameterType="long" resultMap="WaitingSettingResultMap">
	    SELECT
	        setting_id,
	        store_id,
	        day_of_week,
	        -- time_slot,  max_teams,
	        is_active,
	        created_at,  updated_at
	    FROM waiting_settings
	    WHERE setting_id = #{settingId}
	</select>

    <insert id="insertSetting" parameterType="com.spring.teamProject.vo.WaitingSettingVO">
        INSERT INTO waiting_settings (
            store_id,
            day_of_week,
            -- time_slot,  max_teams,
            is_active
        )
        VALUES (
            #{storeId},
            #{dayOfWeek},
            -- #{timeSlot}, #{maxTeams},
            #{active}
        )
    </insert>

    <update id="updateSetting" parameterType="com.spring.teamProject.vo.WaitingSettingVO">
        UPDATE waiting_settings SET
            max_teams = #{maxTeams},
            is_active = #{active},
            updated_at = CURRENT_TIMESTAMP
        WHERE setting_id = #{settingId}
    </update>

    <delete id="deleteSetting" parameterType="long">
        DELETE FROM waiting_settings WHERE setting_id = #{settingId}
    </delete>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.teamProject.dao.WaitingDAO">
	<resultMap id="waitinglist" type="WaitingVO">
		<result property="waiting_id" column="waiting_id"/>
		<result property="store_id" column="store_id"/>
		<result property="day_of_week" column="day_of_week"/>
		<result property="time_slot" column="time_slot"/>
		<result property="max_teams" column="max_teams"/>
		<result property="is_active" column="is_active"/>
	</resultMap>
	
    <insert id="insertWaitingSlot" parameterType="WaitingVO">
        INSERT INTO waiting (
            store_id, day_of_week, time_slot, max_teams, is_active
        ) VALUES (
            #{storeId}, #{dayOfWeek}, #{timeSlot}, #{maxTeams}, #{isActive}
        )
    </insert>

    <select id="getWaitingSlotsByStore" parameterType="long" resultType="WaitingVO">
        SELECT * FROM waiting
        WHERE store_id = #{storeId}
        ORDER BY day_of_week, time_slot
    </select>

    <delete id="deleteWaitingSlot" parameterType="long">
        DELETE FROM waiting WHERE waiting_id = #{waitingId}
    </delete>

    <update id="updateWaitingSlot" parameterType="WaitingVO">
        UPDATE waiting
        SET day_of_week = #{dayOfWeek},
            time_slot = #{timeSlot},
            max_teams = #{maxTeams},
            is_active = #{isActive}
        WHERE waiting_id = #{waitingId}
    </update>

</mapper>
